version: '3.7'
services:
  dev_container:
    build:
      context: .
      dockerfile: .devcontainer/Dockerfile

    volumes:
      - .:/workspace:cached
    init: true
    command: /bin/sh -c "while sleep 1000; do :; done"
    network_mode: host
